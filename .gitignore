# ──────────────── 敏感数据（绝对不能提交！）────────────────
# 注册后的账号凭证 / 接码号池 / OTP 日志
webui/webui.db
webui/webui.db-shm
webui/webui.db-wal
webui/webui.db-journal
webui/logs/
run.log
run*.log
account_*.json
*.session
*.token

# Outlook 4 段格式接码号（如果主人手动放过）
mail.txt
accounts.txt
outlook_accounts.txt

# ──────────────── Python ────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.installed.cfg
*.egg

# 虚拟环境
.venv/
venv/
env/
ENV/

# ──────────────── 测试 / 临时 ────────────────
/tmp/
.pytest_cache/
.coverage
htmlcov/
.tox/

# ──────────────── IDE ────────────────
.vscode/
.idea/
*.swp
*.swo

# ──────────────── OS ────────────────
.DS_Store
Thumbs.db
desktop.ini

# ──────────────── Camoufox / Playwright 缓存（如果主人保留浏览器路径残留）────────────────
.camoufox/
.playwright/
chatgpt_reg_*

# ──────────────── 节点（QuickJS sentinel SDK 缓存）────────────────
node_modules/
openai-sentinel-demo/

# ──────────────── 工作流追踪文件（如果主人启过 AUTH_TRACE_DUMP=1）────────────────
outputs/
auth_trace_*.jsonl

# ──────────────── 配置覆盖（用户本地配置不进 repo）────────────────
config.local.json
.env
.env.local
secrets.json

# ──────────────── 本地接码 / SUB2API 测试产物（含账号凭证，禁止提交）────────────────
email_api_result_*.json
sub2api_import_codex_session_*.json
sub2api_oauth_payload_*.json
manual_otp_result_*.json
